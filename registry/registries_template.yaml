mirrors:
  docker.io:
    endpoint:
      - "https://REGISTRY:443"
  REGISTRY:
    endpoint:
      - "https://REGISTRY:443"
configs:
  "REGISTRY:443":
    auth:
      username: admin # this is the registry username
      password: registry # this is the registry password
    tls:
      insecure_skip_verify: true
